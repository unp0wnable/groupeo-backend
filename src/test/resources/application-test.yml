logging:
  level:
    org:
      hibernate:
        SQL: error

spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/groupeo
    username: groupeo
    password: rucbanfm
    tomcat:
      max-active: 1
      max-idle: 1
      remove-abandoned: true
      remove-abandoned-timeout: 5
